import ComponenteLayoutMsg from "./msg";

export default function ComponenteLayoutPadrao(props) {
    return(
        <>
        {
            (props?.carregando === true) ? (<>
                <ComponenteLayoutMsg icone = {} titulo = {} subtitulo = {} btnTenteNovamente = {true}></ComponenteLayoutMsg>
            </>) : (<>
            {
             (props?.erro) ? (<>
             </>) : (<>
             {(pops?.msg) ? (<>
             </>) : (<>
                {props.children}
             </>)
            }
             </>)    
            }
            </>)
        }
        </>)
}